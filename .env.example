# ============================================
# NFX-Vault 环境配置示例
# 使用方法：复制此文件为 .env，然后根据实际情况修改配置
# ============================================

# ============================================
# 端口配置（可选，有默认值）
# ============================================
BACKEND_PORT=10200
FRONTEND_PORT=10199

# ============================================
# Traefik 证书申请配置（必需）
# ============================================
CERTS_YAML=/volume1/Websites/dynamic/certs.yml
CERT_MAX_WAIT_TIME=300
CERTS_DIR=/volume1/Certs
ACME_CHALLENGE_DIR=/tmp/acme-challenges

# ============================================
# MySQL 数据库配置
# ============================================
# MySQL 服务器地址（可以是 IP 或域名）
MYSQL_HOST=your_mysql_host
# MySQL 端口
MYSQL_DATABASE_PORT=3306
# MySQL root 用户名
MYSQL_ROOT_USERNAME=root
# MySQL root 密码（请修改为强密码）
MYSQL_ROOT_PASSWORD=your_mysql_password

# ============================================
# Redis 配置
# ============================================
# Redis 服务器地址（可以是 IP 或域名）
REDIS_HOST=your_redis_host
# Redis 端口
REDIS_DATABASE_PORT=6379
# Redis 数据库编号（0-15）
REDIS_DB=0
# Redis 密码（如果设置了密码）
REDIS_PASSWORD=your_redis_password
# Redis 缓存过期时间（秒）
REDIS_CACHE_TTL=3600

# ============================================
# Kafka 配置（建议统一命名格式）
# ============================================
# 是否启用 Kafka（true/false）
KAFKA_ENABLED=true
# Kafka 服务器地址（格式：host:port）
KAFKA_BOOTSTRAP_SERVERS=your_kafka_host:9092
# Kafka 事件主题名称
KAFKA_EVENT_TOPIC=nfxvault.cert_server
# Kafka 死信队列主题名称
KAFKA_EVENT_POISON_TOPIC=nfxvault.cert_server.poison
# Kafka 消费者组 ID
KAFKA_CONSUMER_GROUP_ID=nfxvault-cert-server
