/** EmptyScene / EmptyState illustration variants (product-local). */
export enum EmptySceneVariantEnum {
  ABSTRACT = "abstract",
  CAMPFIRE = "campfire",
}

/** LoadingScene / Suspense illustration variants. */
export enum LoadingSceneVariantEnum {
  GOO = "goo",
  FIRE = "fire",
}
